spring:
  application:
    name: sale-service
  cloud:
    config:
      uri: http://localhost:1111
  rabbitmq:
    host: localhost
    password: guest
    port: 15672
    username: guest
    exchange: articles.exchange
    queue: articles.queue
    routingkey: articles.routingkey
